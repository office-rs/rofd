# P1 手型工具 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 实现 WPS 风格手型工具：空白处拖拽平移视口（两轴、clamp 到内容边界），点选批注复用现有选中/移动/缩放/Delete 逻辑，光标经回调（Grab/Grabbing）由各平台适配器映射。

**Architecture:** `Tool` 枚举加 `Hand`；`DragState` 加 `Pan`；滚动 clamp 抽成 render 层共享函数 `clamp_scroll`（几何基于 `composite::page_origin` 的布局公式）；光标状态是 component 纯 UI 态，经 `on_pointer_cursor(PointerCursor)` 回调上抛，web 侧映射 CSS cursor，native 侧走 xilem 声明式 `WindowOptions::with_cursor`。

**Tech Stack:** Rust（rofd workspace）、xilem/masonry_winit @ git rev `bf81712d44e3`、wasm-bindgen、TypeScript SDK（Vite）。

**Spec:** [`docs/superpowers/specs/2026-08-16-ofd-tools-design.md`](../specs/2026-08-16-ofd-tools-design.md) §3（P1 手型工具）。

## Global Constraints

- component 及以下零平台依赖（AGENTS.md §4.9）：不引入 winit/web-sys/arboard 到 `rofd-component`。
- 库不取系统时间（AGENTS.md §4.4）：不动任何 `Date::now`/`SystemTime`。
- clamp 逻辑单一实现（spec §3.1）：只有 render 的 `clamp_scroll` 一份，component 调用它。
- 本计划不改 io；手术后往返测试（`crates/io/tests/round_trip.rs`、`save_surgical.rs`）必须保持绿。
- 提交遵循 conventional commits（`feat`/`fix`/`refactor`/`test`/`chore`）；禁止 attribution 尾注。
- 每个 TDD 步骤先跑测试确认失败（RED）再实现（GREEN）。命令均在仓库根 `D:\code\rofd` 执行。
- 已核实的框架事实（不要重新探索，直接用）：
  - `composite::page_origin`（`crates/render/src/composite.rs:59`）：`page_x = max(0,(size.0-page_w)/2) + scroll.0`；`page_i_y = page_gap - scroll.1 + Σ_{j<i}(h_j + gap)`。
  - xilem@`bf81712` 的 `xilem::window(WindowId, title, root_view)` 返回 `WindowView`，`.with_options(|o| o.with_cursor(CursorIcon))` 在 app_logic 重跑时 diff 并调 `window.set_cursor`（`xilem/src/window_options.rs:218-219`）。app_logic 只在 widget Action（如工具栏点击）后重跑。
  - `masonry_winit::app::MasonryState` **没有**公开的 winit window 访问器（已核对全部 `pub fn`）；直接 `window.set_cursor` 不可行。
  - wasm 侧回调桥：`setup_bridge_callbacks`（`crates/web-view/src/wasm_editor.rs:484`）+ `call_js1_str`（同文件 :579）。

---

### Task 1: render 层 `clamp_scroll` 共享辅助函数

**Files:**
- Modify: `crates/render/src/viewport.rs`（实现 + 单元测试放同文件 `#[cfg(test)]`，跟随该 crate 现有风格）

**Interfaces:**
- Consumes: `Viewport`（现有）、`OfdDocument`/`Page`（dom）。
- Produces: `pub fn clamp_scroll(doc: &OfdDocument, vp: &Viewport) -> (f64, f64)`（Task 2 的 Pan 拖拽调用）。导出到 `rofd_render`（viewport.rs 模块已是 pub，函数加 pub 即可）。

- [ ] **Step 1: 写失败测试**

在 `crates/render/src/viewport.rs` 末尾追加：

```rust
#[cfg(test)]
mod clamp_tests {
    use super::*;
    use crate::{OfdDocument, Page, PageId};
    use crate::primitives::{PageId as _PageIdUnused, Rect};

    fn doc_of(pages: &[(f64, f64)]) -> OfdDocument {
        let mut doc = OfdDocument::default();
        for (i, &(w, h)) in pages.iter().enumerate() {
            doc.pages.push(Page {
                id: PageId::new(format!("P{i}")),
                physical_box: Rect { x: 0.0, y: 0.0, w, h },
                layers: vec![],
                template: None,
            });
        }
        doc
    }

    fn vp(size: (f64, f64), zoom: f64, scroll: (f64, f64)) -> Viewport {
        Viewport { scroll, zoom, size, page_gap: 20.0 }
    }

    #[test]
    fn clamps_both_axes_when_content_exceeds_viewport() {
        // 两页 400x300mm，zoom=2 -> 每页 800x600px，视口 500x700，gap=20。
        // X: 最宽页 800 > 500 -> 余量 (800-500)/2 = 150 -> x ∈ [-150, 150]。
        // Y: 内容高 = 600*2 + 20 = 1220；y_max = gap + 1220 - 700 = 540 -> y ∈ [0, 540]。
        let doc = doc_of(&[(400.0, 300.0), (400.0, 300.0)]);
        assert_eq!(clamp_scroll(&doc, &vp((500.0, 700.0), 2.0, (500.0, 1000.0))), (150.0, 540.0));
        assert_eq!(clamp_scroll(&doc, &vp((500.0, 700.0), 2.0, (-500.0, -5.0))), (-150.0, 0.0));
    }

    #[test]
    fn within_bounds_scroll_unchanged() {
        let doc = doc_of(&[(400.0, 300.0), (400.0, 300.0)]);
        assert_eq!(clamp_scroll(&doc, &vp((500.0, 700.0), 2.0, (50.0, 200.0))), (50.0, 200.0));
    }

    #[test]
    fn pins_to_center_and_top_when_content_fits() {
        // 单页 200x300，zoom=1，视口 500x700：页比视口窄 -> x 钉 0（居中）；
        // 内容高 300 + 20 < 700 -> y 钉 0（顶部对齐）。
        let doc = doc_of(&[(200.0, 300.0)]);
        assert_eq!(clamp_scroll(&doc, &vp((500.0, 700.0), 1.0, (300.0, 500.0))), (0.0, 0.0));
    }

    #[test]
    fn empty_doc_pins_to_zero() {
        let doc = OfdDocument::default();
        assert_eq!(clamp_scroll(&doc, &vp((500.0, 700.0), 1.0, (100.0, 100.0))), (0.0, 0.0));
    }
}
```

注意：import 路径以该文件现有 `use` 为准对齐（`OfdDocument`/`Page`/`PageId`/`Rect` 若已有 re-export 就直接用；没有就按 crate 内其他文件如 `composite.rs` 的用法修正）。上面刻意多余的 `PageId as _PageIdUnused` 行不要粘贴。

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p rofd-render clamp`
Expected: FAIL，报 `clamp_scroll` 未定义。

- [ ] **Step 3: 实现**

在 `viewport.rs` 的 `Viewport` 结构体定义之后追加：

```rust
/// Clamp a viewport scroll offset so the page stack covers the viewport as
/// far as possible (paper-on-desk: the paper can never be scrolled entirely
/// off the desk).
///
/// X uses the widest page: when every page is narrower than the viewport the
/// stack stays centered and `scroll.0` pins to 0. Y allows scrolling from the
/// initial top position (`scroll.1 == 0`) down to the last page's bottom
/// edge reaching the viewport bottom; when the whole stack is shorter than
/// the viewport, `scroll.1` pins to 0.
///
/// Geometry mirrors [`crate::composite::page_origin`]: `page_x = max(0,
/// (size.0 - page_w) / 2) + scroll.0`, `page_y = page_gap - scroll.1 + ...`.
/// Shared single implementation (spec §3.1) - the hand tool's pan drag calls
/// this; wheel `Scroll` may adopt it later.
pub fn clamp_scroll(doc: &OfdDocument, vp: &Viewport) -> (f64, f64) {
    if doc.pages.is_empty() {
        return (0.0, 0.0);
    }
    let widest = doc
        .pages
        .iter()
        .map(|p| p.physical_box.w * vp.zoom)
        .fold(f64::MIN, f64::max);
    let x_margin = (widest - vp.size.0) / 2.0;
    let x = if x_margin <= 0.0 {
        0.0
    } else {
        vp.scroll.0.clamp(-x_margin, x_margin)
    };
    let inner_h: f64 = doc
        .pages
        .iter()
        .map(|p| p.physical_box.h * vp.zoom)
        .sum::<f64>()
        + vp.page_gap * doc.pages.len().saturating_sub(1) as f64;
    let y_max = (vp.page_gap + inner_h - vp.size.1).max(0.0);
    let y = vp.scroll.1.clamp(0.0, y_max);
    (x, y)
}
```

并在 `crates/render/src/lib.rs` 的模块 re-export 处（`Viewport` 被导出的同一位置）加上 `clamp_scroll`（跟随现有 `pub use viewport::...` 的写法）。

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p rofd-render clamp`
Expected: PASS（4 个测试全绿）。

- [ ] **Step 5: 提交**

```bash
git add crates/render/src/viewport.rs crates/render/src/lib.rs
git commit -m "feat(render): add clamp_scroll viewport bounds helper"
```

---

### Task 2: component 层 `Tool::Hand` + `DragState::Pan` 拖拽平移

**Files:**
- Modify: `crates/component/src/editor_component.rs`（`Tool`/`DragState` 定义 ~L18-106、`handle_event` PointerDown/PointerMove/PointerUp、`drag_to_preview` ~L1054 起、`set_tool` ~L204、测试模块）

**Interfaces:**
- Consumes: `rofd_render::clamp_scroll`（Task 1）、现有 `hit_test`/`DragState::Move|Resize|Create`/`maybe_fire_page_change`。
- Produces: `Tool::Hand`（Task 4 `parse_tool_kind("hand")`、Task 5 工具栏按钮使用）；`DragState::Pan { last: (f64, f64) }`。

- [ ] **Step 1: 写失败测试**

在 `editor_component.rs` 测试模块（`mod tests`，已有 `component_with_note`/`component_with_page` 辅助）追加。先加一个两页辅助函数：

```rust
fn component_two_pages() -> EditorComponent {
    // 两页 200x200，vp size (0,0)、gap 0、zoom 1、scroll (0,0)：
    // page0 占 y [0,200)，page1 占 y [200,400)。
    let mut c = EditorComponent::new(EditorConfig::new(Arc::new(vec![])));
    let mut doc = OfdDocument::default();
    for i in 0..2 {
        doc.pages.push(Page {
            id: PageId::new(format!("P{i}")),
            physical_box: Rect { x: 0.0, y: 0.0, w: 200.0, h: 200.0 },
            layers: vec![Layer::default()],
            template: None,
        });
    }
    c.load_document(doc);
    c.viewport = rofd_render::Viewport {
        scroll: (0.0, 0.0),
        zoom: 1.0,
        size: (0.0, 0.0),
        page_gap: 0.0,
    };
    c
}
```

测试用例（全部追加到 `mod tests`；`pan` 事件序列 = PointerDown(Left, 起点) + 若干 PointerMove + PointerUp(Left)）：

```rust
fn pan(c: &mut EditorComponent, from: (f64, f64), to: (f64, f64)) {
    c.handle_event(&ViewEvent::PointerDown {
        button: MouseButton::Left, x: from.0, y: from.1, modifiers: Modifiers::default(),
    });
    c.handle_event(&ViewEvent::PointerMove { x: to.0, y: to.1 });
    c.handle_event(&ViewEvent::PointerUp { button: MouseButton::Left, x: to.0, y: to.1 });
}

#[test]
fn hand_pan_drag_updates_scroll_with_clamp() {
    // component_with_page: 单页 200x200, vp size (0,0), gap 0 -> x ∈ [-100,100], y ∈ [0,200]。
    let mut c = component_with_page();
    c.set_tool(Tool::Hand);
    pan(&mut c, (10.0, 10.0), (30.0, 40.0));
    // dx=+20, dy=+30（指针向下拖 = 内容向下 = scroll.1 减小，clamp 到 0）
    assert_eq!(c.viewport.scroll, (20.0, 0.0));
    pan(&mut c, (10.0, 10.0), (-10.0, -30.0));
    // 上一轮 scroll=(20,0) 为基准：dx=-40, dy=-70 -> x=-20, y=70
    assert_eq!(c.viewport.scroll, (-20.0, 70.0));
}

#[test]
fn hand_pan_clamps_at_max_bounds() {
    let mut c = component_with_page();
    c.set_tool(Tool::Hand);
    // 向上拖出界：scroll.1 上限 200（内容底部对齐视口底，size=0 即页底）。
    pan(&mut c, (10.0, 10.0), (10.0, -10000.0));
    assert_eq!(c.viewport.scroll, (0.0, 200.0));
}

#[test]
fn hand_pan_creates_no_undo_record() {
    let mut c = component_with_page();
    c.set_tool(Tool::Hand);
    pan(&mut c, (10.0, 10.0), (40.0, -20.0));
    assert!(!c.can_undo(), "pan is a view change, not a doc change");
    assert!(!c.is_modified());
}

#[test]
fn hand_pan_fires_page_change_when_crossing_page() {
    let mut c = component_two_pages();
    c.set_tool(Tool::Hand);
    let fired = Arc::new(Mutex::new(None));
    let f = fired.clone();
    c.on_page_change(move |idx| *f.lock().unwrap() = Some(idx));
    // 向上拖 220px：scroll.1=220，视口中心(0)落在内容 y=220 -> page1。
    pan(&mut c, (10.0, 10.0), (10.0, -210.0));
    assert_eq!(*fired.lock().unwrap(), Some(1));
}

#[test]
fn hand_click_annotation_selects_and_delete_removes() {
    // Markup（无文本光标语义）批注：手型下点选 -> Single 选中 -> Delete 删除。
    let mut c = component_with_page();
    c.set_clock("t".into(), 1);
    let id = c.editor.create_annotation(
        AnnotationKind::Highlight,
        PageId::new("P0"),
        AnnotationPayload::Markup {
            quad_points: vec![Point { x: 0.0, y: 0.0 }, Point { x: 100.0, y: 100.0 }],
            color: Color::Rgb(255, 255, 0),
        },
    );
    c.set_tool(Tool::Hand);
    c.handle_event(&ViewEvent::PointerDown {
        button: MouseButton::Left, x: 10.0, y: 10.0, modifiers: Modifiers::default(),
    });
    assert_eq!(c.selection(), &AnnotationSelection::Single(id.clone()));
    // 点在批注上不进入 Pan（drag 是 Move，不是 Pan）
    assert!(matches!(c.drag, Some(DragState::Move { .. })));
    c.handle_event(&ViewEvent::PointerUp { button: MouseButton::Left, x: 10.0, y: 10.0 });
    c.handle_event(&ViewEvent::KeyDown {
        key: Key::Delete, modifiers: Modifiers::default(),
    });
    assert!(c.document().annotations.find(&id).is_none(), "Delete removed the annotation");
}

#[test]
fn switching_tool_cancels_pan_drag() {
    let mut c = component_with_page();
    c.set_tool(Tool::Hand);
    c.handle_event(&ViewEvent::PointerDown {
        button: MouseButton::Left, x: 10.0, y: 10.0, modifiers: Modifiers::default(),
    });
    assert!(matches!(c.drag, Some(DragState::Pan { .. })));
    c.set_tool(Tool::Select);
    assert!(c.drag.is_none(), "set_tool cancels in-progress pan");
}
```

注意：若 `c.drag`/`c.editor`/`c.selection()` 因可见性无法从测试访问，测试与实现同文件（`mod tests` + `use super::*`），`pub(crate)` 字段可直接访问，无需改动可见性。

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p rofd-component hand`
Expected: FAIL，报 `Tool::Hand` / `DragState::Pan` 不存在。

- [ ] **Step 3: 实现**

3a. `Tool` 枚举（~L21）加变体：

```rust
pub enum Tool {
    Select,
    Create(AnnotationKind),
    /// WPS-style hand tool: drag on blank desk/page pans the viewport;
    /// clicking an annotation selects it (move/resize/Delete reuse the
    /// Select interactions).
    Hand,
}
```

3b. `DragState` 加变体（放在 `Resize` 之后）：

```rust
    /// Hand-tool pan drag. Pointer delta maps to viewport scroll (inverted
    /// on y: dragging the pointer down moves content down). View-only; no
    /// document mutation and no undo record.
    Pan {
        last: (f64, f64),
    },
```

3c. 抽取共享方法。把现有 `Tool::Select =>` 分支体（L291-395：hit_test + `HitTarget::Handle`/`Annotation|AnnotationText`/`_ =>` 三路）整体移入新私有方法，`_ =>` 分支只清选区并返回 false：

```rust
/// Left-button PointerDown shared by Select and Hand: hit-test and run the
/// annotation interaction (handle drag / annotation move+select). Returns
/// true when the press landed on an annotation or handle; false when it hit
/// a page or the desk background. Does NOT clear the selection itself --
/// the caller decides what a blank press means (Select: clear; Hand: clear
/// + start pan).
fn pointer_down_annotation(&mut self, p: (f64, f64)) -> bool {
    let target = rofd_render::hit_test(
        self.editor.document(),
        &self.viewport,
        self.editor.selection(),
        p,
    );
    match target {
        rofd_render::HitTarget::Handle(id, h) => {
            // …… 现有 L299-351 的 Handle 分支体原样搬入（选中/预览/Resize drag 设置），不变 ……
            true
        }
        rofd_render::HitTarget::Annotation(id)
        | rofd_render::HitTarget::AnnotationText(id, _) => {
            // …… 现有 L353-385 的 Annotation 分支体原样搬入（选中/设文本光标/Move drag 设置），不变 ……
            true
        }
        _ => false,
    }
}

/// Clear annotation selection + text cursor and fire the change callbacks
/// (shared by Select's and Hand's blank-press paths).
fn clear_selection_and_cursor(&mut self) {
    self.editor.clear_selection();
    self.editor.clear_cursor();
    self.fire_selection_change();
    self.fire_cursor_change();
}
```

3d. `handle_event` 的 `PointerDown Left` match（L281 `match &self.tool`）改为：

```rust
match &self.tool {
    Tool::Create(kind) => {
        self.drag = Some(DragState::Create {
            kind: kind.clone(),
            start: p,
            current: p,
            path: vec![p],
        });
    }
    Tool::Select => {
        if !self.pointer_down_annotation(p) {
            self.clear_selection_and_cursor();
        }
    }
    Tool::Hand => {
        if !self.pointer_down_annotation(p) {
            self.clear_selection_and_cursor();
            self.drag = Some(DragState::Pan { last: p });
        }
    }
}
```

3e. `PointerMove` 的 drag match 加 arm（在 `Some(DragState::Resize {...})` 之后）：

```rust
Some(DragState::Pan { last }) => {
    // Content follows the pointer: x 同向，y 反向（page_y = base - scroll.1）。
    self.viewport.scroll.0 += p.0 - last.0;
    self.viewport.scroll.1 -= p.1 - last.1;
    self.viewport.scroll =
        rofd_render::clamp_scroll(self.editor.document(), &self.viewport);
    self.maybe_fire_page_change();
    *last = p;
}
```

3f. `PointerUp` 的 `match drag` 加 arm（语义：平移结束，无文档变更）：

```rust
DragState::Pan { last: _ } => {
    // View-only pan: nothing to commit.
}
```

3g. `drag_to_preview`（~L1054，把 `&DragState` 映射为预览的函数）加 arm：

```rust
DragState::Pan { .. } => None,
```

（若该函数对 `DragState` 是穷尽 match，编译器会强制加上；Pan 无预览。）

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p rofd-component hand`
Expected: PASS（6 个新测试全绿）。

Run: `cargo test -p rofd-component`
Expected: PASS（既有 Select/Create 测试不被重构破坏）。

- [ ] **Step 5: 提交**

```bash
git add crates/component/src/editor_component.rs
git commit -m "feat(component): hand tool with clamped pan drag"
```

---

### Task 3: component 层 `PointerCursor` 回调

**Files:**
- Modify: `crates/component/src/callbacks.rs`（类型别名 + `Callbacks` 字段 + 测试）
- Modify: `crates/component/src/editor_component.rs`（字段、`set_pointer_cursor`、fire 点、`on_pointer_cursor` 注册器、测试）
- Modify: `crates/component/src/lib.rs`（导出 `PointerCursor`）

**Interfaces:**
- Consumes: Task 2 的 `Tool::Hand`/`DragState::Pan`。
- Produces: `rofd_component::PointerCursor`（Task 4 wasm 桥、Task 5 native 映射使用）；`EditorComponent::on_pointer_cursor(cb: impl Fn(PointerCursor) + 'static [+ Send])`；`EditorComponent::pointer_cursor(&self) -> PointerCursor`。

- [ ] **Step 1: 写失败测试**

`callbacks.rs` 测试模块追加：

```rust
#[test]
fn on_pointer_cursor_fires() {
    use std::sync::{Arc, Mutex};
    let fired = Arc::new(Mutex::new(None));
    let f = fired.clone();
    let cbs = Callbacks {
        on_pointer_cursor: Some(Box::new(move |c: super::PointerCursor| {
            *f.lock().unwrap() = Some(c);
        })),
        ..Default::default()
    };
    (cbs.on_pointer_cursor.as_ref().unwrap())(super::PointerCursor::Grab);
    assert_eq!(*fired.lock().unwrap(), Some(super::PointerCursor::Grab));
}
```

`editor_component.rs` 测试模块追加：

```rust
#[test]
fn hand_tool_cursor_transitions() {
    let fired = Arc::new(Mutex::new(Vec::new()));
    let f = fired.clone();
    let mut c = component_with_page();
    c.on_pointer_cursor(move |cur| f.lock().unwrap().push(cur));
    c.set_tool(Tool::Hand);
    assert_eq!(*fired.lock().unwrap(), vec![PointerCursor::Grab]);
    // 按下空白 -> Grabbing；拖动多帧不重复发；抬起 -> Grab。
    c.handle_event(&ViewEvent::PointerDown {
        button: MouseButton::Left, x: 10.0, y: 10.0, modifiers: Modifiers::default(),
    });
    c.handle_event(&ViewEvent::PointerMove { x: 20.0, y: 20.0 });
    c.handle_event(&ViewEvent::PointerMove { x: 30.0, y: 30.0 });
    c.handle_event(&ViewEvent::PointerUp { button: MouseButton::Left, x: 30.0, y: 30.0 });
    assert_eq!(
        *fired.lock().unwrap(),
        vec![PointerCursor::Grab, PointerCursor::Grabbing, PointerCursor::Grab]
    );
}

#[test]
fn switching_away_from_hand_fires_default() {
    let fired = Arc::new(Mutex::new(Vec::new()));
    let f = fired.clone();
    let mut c = component_with_page();
    c.on_pointer_cursor(move |cur| f.lock().unwrap().push(cur));
    c.set_tool(Tool::Hand);
    c.set_tool(Tool::Select);
    assert_eq!(
        *fired.lock().unwrap(),
        vec![PointerCursor::Grab, PointerCursor::Default]
    );
}
```

（`PointerCursor` 需要 `Debug + PartialEq` 供 `assert_eq!` 使用。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p rofd-component pointer_cursor`
Expected: FAIL，报 `PointerCursor` / `on_pointer_cursor` 未定义。

- [ ] **Step 3: 实现**

3a. `callbacks.rs`：

```rust
/// OS/pointer cursor shape the host should display over the canvas.
/// Platform mapping lives in the adapters (winit CursorIcon / CSS cursor),
/// per AGENTS.md §4.9 (component never touches platform facilities).
#[derive(Debug, Clone, Copy, PartialEq, Eq, Default)]
pub enum PointerCursor {
    #[default]
    Default,
    /// Hand tool hovering (WPS 手型).
    Grab,
    /// Hand tool actively dragging (pan in progress).
    Grabbing,
}
```

类型别名（照 `OnWarning` 的 cfg 双份模式，加在 `OnZoomChange` 之后）：

```rust
#[cfg(not(target_arch = "wasm32"))]
pub type OnPointerCursor = dyn Fn(PointerCursor) + Send;
#[cfg(target_arch = "wasm32")]
pub type OnPointerCursor = dyn Fn(PointerCursor);
```

`Callbacks` 结构体加字段：`pub on_pointer_cursor: Option<Box<OnPointerCursor>>,`

3b. `editor_component.rs`：

- `EditorComponent` 加字段（`tool` 字段旁）：`pub(crate) pointer_cursor: PointerCursor,`，并在 `new()` 的构造处初始化为 `PointerCursor::Default`。
- 私有 fire 辅助（回调去重：仅变化时发）：

```rust
/// Update the pointer-cursor UI state and fire `on_pointer_cursor` when it
/// actually changed (mirrors the zoom-change guard).
fn set_pointer_cursor(&mut self, cursor: PointerCursor) {
    if self.pointer_cursor != cursor {
        self.pointer_cursor = cursor;
        if let Some(cb) = &self.callbacks.on_pointer_cursor {
            cb(cursor);
        }
    }
}
```

- 公开访问器 + 注册器（照 `on_warning` 的 cfg 双份模式，放 `on_warning` 旁）：

```rust
pub fn pointer_cursor(&self) -> PointerCursor {
    self.pointer_cursor
}
#[cfg(not(target_arch = "wasm32"))]
pub fn on_pointer_cursor(&mut self, cb: impl Fn(PointerCursor) + 'static + Send) {
    self.callbacks.on_pointer_cursor = Some(Box::new(cb));
}
#[cfg(target_arch = "wasm32")]
pub fn on_pointer_cursor(&mut self, cb: impl Fn(PointerCursor) + 'static) {
    self.callbacks.on_pointer_cursor = Some(Box::new(cb));
}
```

- fire 点（三处）：
  1. `set_tool`（Task 2 后的现状）末尾追加：

```rust
self.set_pointer_cursor(match self.tool {
    Tool::Hand => PointerCursor::Grab,
    _ => PointerCursor::Default,
});
```

  2. Task 2 的 `Tool::Hand` 空白按下分支，`self.drag = Some(DragState::Pan { last: p });` 之后加：`self.set_pointer_cursor(PointerCursor::Grabbing);`
  3. `PointerUp` 的 `DragState::Pan` arm 内加：`self.set_pointer_cursor(PointerCursor::Grab);`

3c. `lib.rs`：在现有 `pub use callbacks::...` 处加入 `PointerCursor`。

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p rofd-component pointer_cursor`
Expected: PASS。

Run: `cargo test -p rofd-component`
Expected: PASS（Task 2 的 hand 测试不受影响）。

- [ ] **Step 5: 提交**

```bash
git add crates/component/src/callbacks.rs crates/component/src/editor_component.rs crates/component/src/lib.rs
git commit -m "feat(component): pointer cursor callback with grab/grabbing states"
```

---

### Task 4: web-view 桥 + SDK 光标映射 + 手型按钮

**Files:**
- Modify: `crates/web-view/src/wasm_editor.rs`（`parse_tool_kind` ~L55、`pointer_cursor_str` 新纯函数、`JsCallbacks` ~L92、`setup_bridge_callbacks` ~L484、`WasmEditor::set_on_pointer_cursor` 新方法、测试）
- Modify: `crates/web-view/sdk/src/index.ts`（init 中接光标回调）
- Modify: `examples/web-app/src/App.vue`（工具栏手型按钮）

**Interfaces:**
- Consumes: `rofd_component::{Tool::Hand, PointerCursor, EditorComponent::on_pointer_cursor}`（Task 2/3）。
- Produces: `parse_tool_kind("hand") -> Tool::Hand`；`pointer_cursor_str(PointerCursor) -> &'static str`（返回值即合法 CSS cursor 名）；`WasmEditor::set_on_pointer_cursor(Option<js_sys::Function>)`（js 名 `setOnPointerCursor`，单参 string）。

- [ ] **Step 1: 写失败测试**

`wasm_editor.rs`（native 可跑的纯函数测试；若文件已有 `#[cfg(test)] mod tests` 就追加到其中，没有就在文件末尾新建）：

```rust
#[cfg(test)]
mod hand_tool_tests {
    use super::*;
    use rofd_component::{PointerCursor, Tool};

    #[test]
    fn parse_tool_kind_hand() {
        assert_eq!(parse_tool_kind("hand"), Tool::Hand);
    }

    #[test]
    fn pointer_cursor_str_maps_to_css_names() {
        assert_eq!(pointer_cursor_str(PointerCursor::Default), "default");
        assert_eq!(pointer_cursor_str(PointerCursor::Grab), "grab");
        assert_eq!(pointer_cursor_str(PointerCursor::Grabbing), "grabbing");
    }
}
```

（`parse_tool_kind` 在 wasm_impl 之外、`pointer_cursor_str` 也定义在外面（同 `parse_tool_kind` 位置），保证 native `cargo test` 可跑。）

- [ ] **Step 2: 跑测试确认失败**

Run: `cargo test -p rofd-web-view hand_tool`
Expected: FAIL，报 `pointer_cursor_str` 未定义 / `parse_tool_kind("hand")` 返回 Select 断言失败。

- [ ] **Step 3: 实现**

3a. `parse_tool_kind` 加一行（`"select"` 之后）：

```rust
"hand" => Tool::Hand,
```

并更新其 doc 注释的按钮清单（加 hand）。

3b. `parse_tool_kind` 旁新增：

```rust
/// Map a [`PointerCursor`] to its JS-facing string. The values are chosen to
/// be valid CSS cursor names, so the SDK can assign the string straight to
/// `canvas.style.cursor` with no mapping table on the JS side.
pub fn pointer_cursor_str(c: PointerCursor) -> &'static str {
    match c {
        PointerCursor::Default => "default",
        PointerCursor::Grab => "grab",
        PointerCursor::Grabbing => "grabbing",
    }
}
```

（文件顶部 `use rofd_component::...` 处补 `PointerCursor`。）

3c. `JsCallbacks` 加槽位（`on_zoom_change` 之后）：

```rust
pub on_pointer_cursor: Rc<RefCell<Option<js_sys::Function>>>,
```

3d. `setup_bridge_callbacks` 末尾（`on_zoom_change` 接线之后）追加：

```rust
let on_pointer_cursor_js = self.callbacks.on_pointer_cursor.clone();
self.component.on_pointer_cursor(Box::new(move |cursor: PointerCursor| {
    call_js1_str(&on_pointer_cursor_js, pointer_cursor_str(cursor));
}));
```

（`use` 处补 `PointerCursor`。）

3e. `WasmEditor` 加注册方法（照 `set_on_zoom_change` 的模式）：

```rust
#[wasm_bindgen(js_name = setOnPointerCursor)]
pub fn set_on_pointer_cursor(&mut self, callback: Option<js_sys::Function>) {
    *self.callbacks.on_pointer_cursor.borrow_mut() = callback;
}
```

- [ ] **Step 4: 跑测试确认通过**

Run: `cargo test -p rofd-web-view hand_tool`
Expected: PASS。

- [ ] **Step 5: SDK + web-app 接线**

`sdk/src/index.ts`：`Editor.init` 中创建 wasm 实例之后（其他接线所在处）加：

```ts
// Pointer cursor: the wasm side reports CSS cursor names directly
// ("default"/"grab"/"grabbing"), so no mapping is needed here.
wasm.setOnPointerCursor((shape: string) => {
  canvas.style.cursor = shape;
});
```

（`wasm`/`canvas` 为 init 里的局部变量名，以现有代码为准。）

`examples/web-app/src/App.vue`：工具栏“选择”按钮（~L42）之后，照其标记完全相同的结构加：

```html
<ToolButton tooltip="手型" :active="activeTool === 'hand'" @click="setTool('hand')">
  <!-- 图标与“选择”按钮同款结构，换手型图标（lucide 的 Hand） -->
</ToolButton>
```

（ToolButton 的图标槽结构以现有“选择”按钮为准镜像；`setTool('hand')` 无需改 `setTool` 函数——`activeTool` 状态与 `editor.setTool(kind)` 走既有通道，wasm 侧 `parse_tool_kind` 已识别 `hand`。）

- [ ] **Step 6: 构建验证**

```bash
cargo test -p rofd-web-view
cd examples/web-app && npm run build
```

Expected: crate 测试 PASS；vite/tsc 构建 PASS（TS 无类型错误）。

- [ ] **Step 7: 提交**

```bash
git add crates/web-view/src/wasm_editor.rs crates/web-view/sdk/src/index.ts examples/web-app/src/App.vue
git commit -m "feat(web-view): hand tool kind, pointer cursor bridge, sdk css cursor"
```

---

### Task 5: native-app 手型按钮 + 声明式窗口光标

**Files:**
- Modify: `examples/native-app/src/main.rs`（imports、`AppState`、`app_logic`、`main`、`window_event`、工具栏）

**Interfaces:**
- Consumes: `rofd_component::{Tool::Hand, PointerCursor, EditorComponent::on_pointer_cursor}`（Task 2/3）。
- Produces: 无对外接口（demo 应用）。

**框架事实（已核实，直接采用）**：xilem@`bf81712` 提供 `xilem::window(WindowId, title, root) -> WindowView` 与 `WindowView::with_options(|o| o.with_cursor(CursorIcon))`；`WindowOptions::rebuild` 在 app_logic 重跑时 diff cursor 并调 `window.set_cursor`（`xilem/src/window_options.rs:218`）。app_logic 在 widget Action（工具栏点击）后重跑。`MasonryState` 无公开 window 句柄，拖拽中的 Grabbing 实时刷新在本 rev 不可行（见 Step 7 说明）。

- [ ] **Step 1: 工具栏加手型按钮**

在 `btn_select`（~L139）之后加：

```rust
let btn_hand = tool_button("手型", Tool::Hand);
```

并加入 `tool_row` 的 `flex_row((...))` 元组（`btn_select` 之后、`btn_highlight` 之前）。

- [ ] **Step 2: AppState 加光标与窗口 id**

```rust
type SharedPointerCursor = Arc<Mutex<rofd_component::PointerCursor>>;

struct AppState {
    editor: SharedEditor,
    canvas_widget_id: SharedCanvasId,
    wake_proxy: SharedWakeProxy,
    window_id: xilem::WindowId,
    pointer_cursor: SharedPointerCursor,
}
```

并实现 xilem 要求的 state trait（`Xilem::new` 需要）：

```rust
impl xilem::AppState for AppState {
    fn keep_running(&self) -> bool {
        true
    }
}
```

- [ ] **Step 3: app_logic 返回 WindowView（声明式 cursor）**

把 `fn app_logic(_app: &mut AppState) -> impl WidgetView<AppState> + use<>` 改为：先构建现有 root（`flex_col((...))` 原内容不动，绑定为 `let root = ...`），再包裹：

```rust
fn app_logic(app: &mut AppState) -> std::iter::Once<xilem::WindowView<AppState>> {
    // …… 现有 btn_open / tool_row / canvas 等 view 构建，全部不动 ……
    let root = flex_col((/* 现有元组 */))./* 现有链式调用 */;

    let cursor_icon = match *app.pointer_cursor.lock().unwrap() {
        rofd_component::PointerCursor::Default => winit::window::CursorIcon::Default,
        rofd_component::PointerCursor::Grab => winit::window::CursorIcon::Grab,
        rofd_component::PointerCursor::Grabbing => winit::window::CursorIcon::Grabbing,
    };
    std::iter::once(
        xilem::window(app.window_id, "rofd - OFD Editor", root)
            .with_options(|o| o.with_cursor(cursor_icon)),
    )
}
```

- [ ] **Step 4: main() 切换到 `Xilem::new` 并接回调**

在 `main()` 中：

```rust
let window_id = xilem::WindowId::next();
let pointer_cursor: SharedPointerCursor =
    Arc::new(Mutex::new(rofd_component::PointerCursor::Default));
{
    let pc = pointer_cursor.clone();
    editor
        .lock()
        .unwrap()
        .component
        .on_pointer_cursor(move |c| *pc.lock().unwrap() = c);
}

let app_state = AppState {
    editor: editor.clone(),
    canvas_widget_id: canvas_widget_id.clone(),
    wake_proxy: wake_proxy.clone(),
    window_id,
    pointer_cursor: pointer_cursor.clone(),
};

// 原：let window_options = WindowOptions::new("rofd - OFD Editor");
//     let xilem = Xilem::new_simple(app_state, app_logic, window_options);
let xilem = Xilem::new(app_state, app_logic);
```

（`Xilem::new` 内部自带 tokio runtime（`xilem/src/app.rs:134-137` 已核实），`task` view 不受影响；`into_driver_and_windows` 及其后代码不动。）

- [ ] **Step 5: 窗口关闭退出**

`new_simple` 的 `ExitOnClose` 包装没了，`keep_running` 恒 true，需在 `window_event` 顶部（`update_canvas_origin()` 之前）补：

```rust
// Xilem::new（无 ExitOnClose 包装）下自己处理关闭退出。
if matches!(ev, WindowEvent::CloseRequested) {
    el.exit();
}
```

- [ ] **Step 6: 编译 + 手动验证**

```bash
cargo build -p native-app
cargo run -p native-app -- test/ru-yuan-ji-lu.ofd
```

手动检查清单：
1. 工具栏出现“手型”按钮。
2. 点击“手型” -> 窗口光标变抓手（app_logic 重跑 -> with_cursor diff 生效）。
3. 在空白/页面空白处拖拽 -> 文档平移（两轴），拖出界被 clamp，不产生 undo 记录。
4. 手型下点击高亮/手绘批注 -> 出现选中框；Delete -> 删除。
5. 切回“选择” -> 光标恢复默认箭头。
6. 关闭窗口 -> 进程退出。

- [ ] **Step 7: 记录已知限制**

在 `main.rs` 文件头注释块末尾追加一段：

```rust
// Known limitation (hand tool cursor): xilem@bf81712 re-runs app_logic only
// on widget Actions, and MasonryState exposes no winit window handle, so the
// Grabbing cursor cannot refresh mid-drag (Grab/Default update on toolbar
// clicks). Revisit on the next Linebender stack bump.
```

- [ ] **Step 8: 提交**

```bash
git add examples/native-app/src/main.rs
git commit -m "feat(native-app): hand tool button + declarative window cursor"
```

---

### Task 6: 全量回归

**Files:**
- 无新改动（只验证 + 可能的 fmt/clippy 修正）。

- [ ] **Step 1: 全量检查**

```bash
cargo fmt --all -- --check
cargo clippy --workspace --all-targets -- -D warnings
cargo test --workspace
```

Expected: 三条全绿。若 fmt/clippy 有小修正：修后重跑，并用 `chore: fmt/clippy fixes for hand tool` 单独提交。

- [ ] **Step 2: 手术后往返确认（不变量 4.3）**

Task 1-5 未触 io，`cargo test --workspace` 中 `crates/io/tests/round_trip.rs` 与 `save_surgical.rs` 应天然绿；确认它们在输出中出现且 PASS。

- [ ] **Step 3: 提交（如有修正）**

```bash
git add -A && git commit -m "chore: fmt/clippy fixes for hand tool"
```

（无修正则跳过本步。）

---

## Self-Review 记录

- **Spec 覆盖**：spec §3.1 交互（拖拽平移/clamp/批注点选复用/Delete 既有）-> Task 2；§3.2 光标回调与平台映射 -> Task 3/4/5；§3.3 涉及范围 -> Task 4（SDK `setTool('hand')` 即 `parse_tool_kind`）/Task 5 工具栏。spec 说“clamp 从 Scroll 抽取复用”——实际现状 Scroll 无 clamp，本计划新增共享函数并注明 wheel Scroll 采用可作后续（不改变现有滚轮行为，避免大范围测试语义变更）。
- **占位符**：Task 2 Step 3c 的“现有 L299-351 原样搬入”是对既有代码的机械移动（带行号锚点），非新逻辑占位；其余步骤均含完整代码。
- **类型一致性**：`clamp_scroll(doc, vp) -> (f64,f64)`、`Tool::Hand`、`DragState::Pan { last }`、`PointerCursor{Default,Grab,Grabbing}`、`on_pointer_cursor`、`parse_tool_kind("hand")`、`pointer_cursor_str`、`setOnPointerCursor` 在各任务间已对齐。
